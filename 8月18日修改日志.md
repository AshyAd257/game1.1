看了现有代码，目前"进竞技场"这块已经有人写了一半，而且有问题：

现状（PlayerController.enterArena() + 相关改动，均未 commit）：
- 按 M 键触发的 enterArena() 里有个语法错误：if (camera != null) {` 多了个反引号，这段代码现在编译不过
- 竞技场直径写的是 50，不是要求的 80
- 最关键的：它是在同一个世界里，直接对主世界的 ChunkManager 在坐标 (100,100)
  处清空+生成地形，把玩家传送过去。这不是"进入新世界"，而是把主世界的一部分永久性地改写成竞技场地形——不满足"传送到新世界
  "的要求，而且是破坏性的（主世界那块区域的原始地形会被覆盖）
- 没有保存玩家原本所在世界的状态/位置这一步

同时项目里还躺着一个 WorldSwitcher 类（com.Hecate.arena.WorldSwitcher），这个类的设计思路正好是你要的：独立的
ChunkManager + 独立的场景节点，进入竞技场时保存主世界玩家位置、隐藏主世界节点、显示竞技场节点；退出时反向操作。但这个
类目前完全没有被实例化或接入任何地方，是孤立代码。

所以要做成你说的效果（保存当前世界状态 + 传送到一个独立的 80x80 圆形平坦体素世界），比较合理的路子是复用/修好
WorldSwitcher 这套机制，而不是继续用 PlayerController.enterArena()
里那段直接改写主世界的代码。这涉及到几个文件的改动和一个架构选择，我先进 plan 模式把方案理清楚再动手。
已经搞清楚的关键事实：

1. 玩家的落地/碰撞机制是靠地形高度场（HeightMap），不是靠体素方块碰撞。PlayerController 每帧调
   collisionManager.getTerrainHeightAt(x,z)，这个方法要求 chunk 的 hasTerrainData()==true（也就是 surfaceMaterials 不全是
   NONE），否则返回 NaN，玩家就会掉回硬编码的 GROUND_LEVEL=2.75f。真正的体素方块碰撞（isBlockSolid/checkCollision）虽然
   存在，但玩家移动代码里写着"暂时禁用体素碰撞检测，使用地形碰撞系统"（line 1394），实际没在用。
2. ArenaWorld.generateArena() 目前只调用 chunk.setBlock(...) 填方块，完全不碰
   surfaceHeightMap/surfaceMaterials。这意味着它生成的竞技场地面 hasTerrainData() 是
   不全是 NONE），否则返回 NaN，玩家就会掉回硬编码的 GROUND_LEVEL=2.75f。真正的体素方块碰撞（isBlockSolid/checkCollision
   ）虽然存在，但玩家移动代码里写着"暂时禁用体素碰撞检测，使用地形碰撞系统"（line 1394），实际没在用。
2. ArenaWorld.generateArena() 目前只调用 chunk.setBlock(...) 填方块，完全不碰
   surfaceHeightMap/surfaceMaterials。这意味着它生成的竞技场地面 hasTerrainData() 是
   false，玩家踩上去会被判定为"没有地形"，直接掉到硬编码高度
   不全是 NONE），否则返回 NaN，玩家就会掉回硬编码的 GROUND_LEVEL=2.75f。真正的体素方块碰撞（isBlockSolid/checkCollisio
   n）虽然存在，但玩家移动代码里写着"暂时禁用体素碰撞检测，使用地形碰撞系统"（line 1394），实际没在用。
2. ArenaWorld.generateArena() 目前只调用 chunk.setBlock(...) 填方块，完全不碰
   surfaceHeightMap/surfaceMaterials。这意味着它生成的竞技场地面 hasTerrainData() 是
   false，玩家踩上去会被判定为"没有地形"，直接掉到硬编码高度
   2.75，而不是站在竞技场的方块地面上。这是一个必须解决的设计缺口，不只是把 diameter 从 50 改成 80 那么简单。
1. 玩家的落地/碰撞机制是靠地形高度场（HeightMap），不是靠体素方块碰撞。PlayerController 每帧调
   collisionManager.getTerrainHeightAt(x,z)，这个方法要求 chunk 的 hasTerrainData()==true（也就是 surfaceMaterials
   不全是 NONE），否则返回 NaN，玩家就会掉回硬编码的 GROUND_LEVEL=2.75f。真正的体素方块碰撞（isBlockSolid/checkColli
   sion）虽然存在，但玩家移动代码里写着"暂时禁用体素碰撞检测，使用地形碰撞系统"（line 1394），实际没在用。
2. ArenaWorld.generateArena() 目前只调用 chunk.setBlock(...) 填方块，完全不碰
   surfaceHeightMap/surfaceMaterials。这意味着它生成的竞技场地面 hasTerrainData() 是
   false，玩家踩上去会被判定为"没有地形"，直接掉到硬编码高度
   2.75，而不是站在竞技场的方块地面上。这是一个必须解决的设计缺口，不只是把 diameter 从 50 改成 80 那么简单。
3. ChunkManager.loadChunk() 对任何新 chunk 都会自动跑
   1394），实际没在用。
2. ArenaWorld.generateArena() 目前只调用 chunk.setBlock(...) 填方块，完全不碰
   surfaceHeightMap/surfaceMaterials。这意味着它生成的竞技场地面 hasTerrainData() 是
   false，玩家踩上去会被判定为"没有地形"，直接掉到硬编码高度
   2.75，而不是站在竞技场的方块地面上。这是一个必须解决的设计缺口，不只是把 diameter 从 50 改成 80 那么简单。
3. ChunkManager.loadChunk() 对任何新 chunk 都会自动跑
   fillWithTestPattern()，这个方法会生成噪声地形高度场+DIRT材质。ArenaWorld 现在调用的是
1. 玩家的落地/碰撞机制是靠地形高度场（HeightMap），不是靠体素方块碰撞。PlayerController 每帧调
   collisionManager.getTerrainHeightAt(x,z)，这个方法要求 chunk 的 hasTerrainData()==true（也就是
   surfaceMaterials 不全是 NONE），否则返回 NaN，玩家就会掉回硬编码的
   GROUND_LEVEL=2.75f。真正的体素方块碰撞（isBlockSolid/checkCollision）虽然存在，但玩家移动代码里写着"暂时
   禁用体素碰撞检测，使用地形碰撞系统"（line 1394），实际没在用。
2. ArenaWorld.generateArena() 目前只调用 chunk.setBlock(...) 填方块，完全不碰
   surfaceHeightMap/surfaceMaterials。这意味着它生成的竞技场地面 hasTerrainData() 是
   false，玩家踩上去会被判定为"没有地形"，直接掉到硬编码高度
   2.75，而不是站在竞技场的方块地面上。这是一个必须解决的设计缺口，不只是把 diameter 从 50 改成 80
   那么简单。
3. ChunkManager.loadChunk() 对任何新 chunk 都会自动跑
   fillWithTestPattern()，这个方法会生成噪声地形高度场+DIRT材质。ArenaWorld 现在调用的是
   getOrLoadChunk，所以竞技场的地基chunk会先被这个噪声地形污染，再被 setBlock
   覆盖方块——但高度场残留没清，会导致"看起来是方块地面，脚下判定却是波浪噪声地形"的错乱。
4. 渲染管线是私有的、绑死在 WorldModule 自己的 chunkManager 上（WorldModule.renderChunk   已经搞清楚的关键事实：

1. 玩家的落地/碰撞机制是靠地形高度场（HeightMap），不是靠体素方块碰撞。PlayerController每帧调
   collisionManager.getTerrainHeightAt(x,z)，这个方法要求 chunk 的 hasTerrainData()==true（也就是
   surfaceMaterials 不全是 NONE），否则返回 NaN，玩家就会掉回硬编码的
   GROUND_LEVEL=2.75f。真正的体素方块碰撞（isBlockSolid/checkCollision）虽然存在，但玩家移动代码里
   写着"暂时禁用体素碰撞检测，使用地形碰撞系统"（line 1394），实际没在用。
2. ArenaWorld.generateArena() 目前只调用 chunk.setBlock(...) 填方块，完全不碰
   surfaceHeightMap/surfaceMaterials。这意味着它生成的竞技场地面 hasTerrainData() 是
   false，玩家踩上去会被判定为"没有地形"，直接掉到硬编码高度
   2.75，而不是站在竞技场的方块地面上。这是一个必须解决的设计缺口，不只是把 diameter 从 50改成                                                                                     80
   那么简单。
3. ChunkManager.loadChunk() 对任何新 chunk 都会自动跑
   fillWithTestPattern()，这个方法会生成噪声地形高度场+DIRT材质。ArenaWorld 现在调用的是
   getOrLoadChunk，所以竞技场的地基chunk会先被这个噪声地形污染，再被 setBlock
   覆盖方块——但高度场残留没清，会导致"看起来是方块地面，脚下判定却是波浪噪声地形"的错乱。
4. 渲染管线是私有的、绑死在 WorldModule 自己的 chunkManager 上（WorldModule.renderChunk是
   private 方法，只认自己的字段）。ArenaWorld/WorldSwitcher 现在完全没有渲染代码，只改方块数据和切
1. 玩家的落地/碰撞机制是靠地形高度场（HeightMap），不是靠体素方块碰撞。PlayerController
   每帧调 collisionManager.getTerrainHeightAt(x,z)，这个方法要求 chunk 的
   hasTerrainData()==true（也就是 surfaceMaterials 不全是 NONE），否则返回
   NaN，玩家就会掉回硬编码的 GROUND_LEVEL=2.75f。真正的体素方块碰撞（isBlockSolid/checkColl
   ision）虽然存在，但玩家移动代码里写着"暂时禁用体素碰撞检测，使用地形碰撞系统"（line
   1394），实际没在用。
2. ArenaWorld.generateArena() 目前只调用 chunk.setBlock(...) 填方块，完全不碰
   surfaceHeightMap/surfaceMaterials。这意味着它生成的竞技场地面 hasTerrainData()是
   false，玩家踩上去会被判定为"没有地形"，直接掉到硬编码高度
   2.75，而不是站在竞技场的方块地面上。这是一个必须解决的设计缺口，不只是把 diameter                                                                              从                                                                              50
   改成 80 那么简单。
3. ChunkManager.loadChunk() 对任何新 chunk 都会自动跑
   fillWithTestPattern()，这个方法会生成噪声地形高度场+DIRT材质。ArenaWorld 现在调用的是
   getOrLoadChunk，所以竞技场的地基chunk会先被这个噪声地形污染，再被 setBlock
   覆盖方块——但高度场残留没清，会导致"看起来是方块地面，脚下判定却是波浪噪声地形"的错乱。
4. 渲染管线是私有的、绑死在 WorldModule 自己的 chunkManager 上（WorldModule.renderChunk
   是 private 方法，只认自己的字段）。ArenaWorld/WorldSwitcher
   GROUND_LEVEL=2.75f。真正的体素方块碰撞（isBlockSolid/checkCollision）虽然存在，
   但玩家移动代码里写着"暂时禁用体素碰撞检测，使用地形碰撞系统"（line
   1394），实际没在用。
2. ArenaWorld.generateArena() 目前只调用 chunk.setBlock(...) 填方块，完全不碰
   surfaceHeightMap/surfaceMaterials。这意味着它生成的竞技场地面 hasTerrainData()
   是 false，玩家踩上去会被判定为"没有地形"，直接掉到硬编码高度
   2.75，而不是站在竞技场的方块地面上。这是一个必须解决的设计缺口，不只是把
   diameter 从 50 改成 80 那么简单。
3. ChunkManager.loadChunk() 对任何新 chunk 都会自动跑
   fillWithTestPattern()，这个方法会生成噪声地形高度场+DIRT材质。ArenaWorld
   现在调用的是 getOrLoadChunk，所以竞技场的地基chunk会先被这个噪声地形污染，再被
   setBlock 覆盖方块——但高度场残留没清，会导致"看起来是方块地面，脚下判定却是波浪
   噪声地形"的错乱。
4. 渲染管线是私有的、绑死在 WorldModule 自己的 chunkManager
   上（WorldModule.renderChunk 是 private
   方法，只认自己的字段）。ArenaWorld/WorldSwitcher
   现在完全没有渲染代码，只改方块数据和切 CullHint。如果给竞技场用独立
   填方块，完全不碰
   surfaceHeightMap/surfaceMaterials。这意味着它生成的竞技场地面
   hasTerrainData() 是
   false，玩家踩上去会被判定为"没有地形"，直接掉到硬编码高度
   2.75，而不是站在竞技场的方块地面上。这是一个必须解决的设计缺口，不只是把
   diameter 从 50 改成 80 那么简单。
3. ChunkManager.loadChunk() 对任何新 chunk 都会自动跑
   fillWithTestPattern()，这个方法会生成噪声地形高度场+DIRT材质。ArenaWorld
   现在调用的是
   getOrLoadChunk，所以竞技场的地基chunk会先被这个噪声地形污染，再被
   setBlock 覆盖方块——但高度场残留没清，会导致"看起来是方块地面，脚下判定却
   是波浪噪声地形"的错乱。
4. 渲染管线是私有的、绑死在 WorldModule 自己的 chunkManager
   上（WorldModule.renderChunk 是 private
   方法，只认自己的字段）。ArenaWorld/WorldSwitcher
   现在完全没有渲染代码，只改方块数据和切 CullHint。如果给竞技场用独立
   ChunkManager，必须自己写一套渲染驱动（不能直接调用 WorldModule
   的），否则方块数据有了但屏幕上什么都不会显示。
5. 已有的 WorldSwitcher 类（未接入任何地方）的思路是对的：独立
   填方块，完全不碰
   surfaceHeightMap/surfaceMaterials。这意味着它生成的竞技场地面
   hasTerrainData() 是
   false，玩家踩上去会被判定为"没有地形"，直接掉到硬编码高度 2.75，
   而不是站在竞技场的方块地面上。这是一个必须解决的设计缺口，不只是
   把 diameter 从 50 改成 80 那么简单。
3. ChunkManager.loadChunk() 对任何新 chunk 都会自动跑 fillWithTes
   tPattern()，这个方法会生成噪声地形高度场+DIRT材质。ArenaWorld
   现在调用的是
   getOrLoadChunk，所以竞技场的地基chunk会先被这个噪声地形污染，再被
   setBlock 覆盖方块——但高度场残留没清，会导致"看起来是方块地面，脚
   下判定却是波浪噪声地形"的错乱。
4. 渲染管线是私有的、绑死在 WorldModule 自己的 chunkManager
   上（WorldModule.renderChunk 是 private
   方法，只认自己的字段）。ArenaWorld/WorldSwitcher
   现在完全没有渲染代码，只改方块数据和切
   CullHint。如果给竞技场用独立
   ChunkManager，必须自己写一套渲染驱动（不能直接调用 WorldModule
   的），否则方块数据有了但屏幕上什么都不会显示。
5. 已有的 WorldSwitcher 类（未接入任何地方）的思路是对的：独立
   ChunkManager（存档路径自动分离成 saves/arena/ vs
   检测，使用地形碰撞系统"（line 1394），实际没在用。
2. ArenaWorld.generateArena() 目前只调用
   chunk.setBlock(...) 填方块，完全不碰 surfaceHeightMap/surf
   aceMaterials。这意味着它生成的竞技场地面 hasTerrainData()
   是
   false，玩家踩上去会被判定为"没有地形"，直接掉到硬编码高度
   2.75，而不是站在竞技场的方块地面上。这是一个必须解决的设计
   缺口，不只是把 diameter 从 50 改成 80 那么简单。
3. ChunkManager.loadChunk() 对任何新 chunk 都会自动跑
   fillWithTestPattern()，这个方法会生成噪声地形高度场+DIRT材
   质。ArenaWorld 现在调用的是 getOrLoadChunk，所以竞技场的地
   基chunk会先被这个噪声地形污染，再被 setBlock
   覆盖方块——但高度场残留没清，会导致"看起来是方块地面，脚下
   判定却是波浪噪声地形"的错乱。
4. 渲染管线是私有的、绑死在 WorldModule 自己的
   chunkManager 上（WorldModule.renderChunk 是 private
   方法，只认自己的字段）。ArenaWorld/WorldSwitcher
   现在完全没有渲染代码，只改方块数据和切
   CullHint。如果给竞技场用独立
   ChunkManager，必须自己写一套渲染驱动（不能直接调用
   WorldModule 的），否则方块数据有了但屏幕上什么都不会显示。
5. 已有的 WorldSwitcher
   类（未接入任何地方）的思路是对的：独立
   ChunkManager（存档路径自动分离成 saves/arena/ vs
   saves/world/）、独立世界节点、隐藏主世界节点+显示竞技场节
   T材质。ArenaWorld 现在调用的是 getOrLoadChunk，所以竞技
   场的地基chunk会先被这个噪声地形污染，再被 setBlock
   覆盖方块——但高度场残留没清，会导致"看起来是方块地面，脚
   下判定却是波浪噪声地形"的错乱。
4. 渲染管线是私有的、绑死在 WorldModule 自己的
   chunkManager 上（WorldModule.renderChunk 是 private
   方法，只认自己的字段）。ArenaWorld/WorldSwitcher
   现在完全没有渲染代码，只改方块数据和切
   CullHint。如果给竞技场用独立
   ChunkManager，必须自己写一套渲染驱动（不能直接调用
   WorldModule
   的），否则方块数据有了但屏幕上什么都不会显示。
5. 已有的 WorldSwitcher
   类（未接入任何地方）的思路是对的：独立
   ChunkManager（存档路径自动分离成 saves/arena/ vs
   saves/world/）、独立世界节点、隐藏主世界节点+显示竞技场
   setBlock 覆盖方块——但高度场残留没清，会导致"看起来是
   方块地面，脚下判定却是波浪噪声地形"的错乱。
4. 渲染管线是私有的、绑死在 WorldModule 自己的
   chunkManager 上（WorldModule.renderChunk 是 private
   方法，只认自己的字段）。ArenaWorld/WorldSwitcher
   现在完全没有渲染代码，只改方块数据和切
   CullHint。如果给竞技场用独立
   ChunkManager，必须自己写一套渲染驱动（不能直接调用
   WorldModule
   的），否则方块数据有了但屏幕上什么都不会显示。
5. 已有的 WorldSwitcher
   类（未接入任何地方）的思路是对的：独立
   ChunkManager（存档路径自动分离成 saves/arena/ vs
   saves/world/）、独立世界节点、隐藏主世界节点+显示竞技
   场节点、保存/恢复玩家坐标。但它同样没有渲染驱动这块。
   声地形高度场+DIRT材质。ArenaWorld 现在调用的是
   getOrLoadChunk，所以竞技场的地基chunk会先被这个噪
   声地形污染，再被 setBlock
   覆盖方块——但高度场残留没清，会导致"看起来是方块地
   面，脚下判定却是波浪噪声地形"的错乱。
4. 渲染管线是私有的、绑死在 WorldModule 自己的
   chunkManager 上（WorldModule.renderChunk 是
   private
   方法，只认自己的字段）。ArenaWorld/WorldSwitcher
   现在完全没有渲染代码，只改方块数据和切
   CullHint。如果给竞技场用独立
   ChunkManager，必须自己写一套渲染驱动（不能直接调用
   WorldModule
   的），否则方块数据有了但屏幕上什么都不会显示。
5. 已有的 WorldSwitcher
   类（未接入任何地方）的思路是对的：独立
   ChunkManager（存档路径自动分离成 saves/arena/ vs
   saves/world/）、独立世界节点、隐藏主世界节点+显示
   竞技场节点、保存/恢复玩家坐标。但它同样没有渲染驱
   动这块。
6. 现有 WIP 代码
   覆盖方块——但高度场残留没清，会导致"看起来是方块
   地面，脚下判定却是波浪噪声地形"的错乱。
4. 渲染管线是私有的、绑死在 WorldModule 自己的
   chunkManager 上（WorldModule.renderChunk 是
   private 方法，只认自己的字段）。ArenaWorld/Worl
   dSwitcher
   现在完全没有渲染代码，只改方块数据和切
   CullHint。如果给竞技场用独立 ChunkManager，必须
   自己写一套渲染驱动（不能直接调用 WorldModule
   的），否则方块数据有了但屏幕上什么都不会显示。
5. 已有的 WorldSwitcher
   类（未接入任何地方）的思路是对的：独立
   ChunkManager（存档路径自动分离成 saves/arena/
   vs saves/world/）、独立世界节点、隐藏主世界节点
   +显示竞技场节点、保存/恢复玩家坐标。但它同样没
   有渲染驱动这块。
6. 现有 WIP 代码
   PlayerController.enterArena()（按M键，未提交  只认自己的字段）。ArenaWorld/WorldSwitcher
   现在完全没有渲染代码，只改方块数据和切
   CullHint。如果给竞技场用独立 ChunkManager，必
   须自己写一套渲染驱动（不能直接调用
   WorldModule 的），否则方块数据有了但屏幕上什
   么都不会显示。
5. 已有的 WorldSwitcher
   类（未接入任何地方）的思路是对的：独立
   的），否则方块数据有了但屏幕上什么都不会显示。
5. 已有的 WorldSwitcher
   类（未接入任何地方）的思路是对的：独立
   ChunkManager（存档路径自动分离成 saves/arena/
   vs saves/world/）、独立世界节点、隐藏主世界节
   点+显示竞技场节点、保存/恢复玩家坐标。但它同样
   没有渲染驱动这块。
6. 现有 WIP 代码
   PlayerController.enterArena()（按M键，未提交）
   是在主世界原地(100,100)清空重建，语法还有个反
   引号错误编译不过，不是你要的"进新世界"效果。
7. 切换世界还需要联动：CollisionManager.setChu
   nkManager()（否则地形高度查询指向错的世界）、P
   layerController.setWorldNode()（这个会级联到
   puppet 阴影射线检测），PlayerControlModule 的
   BlockInteraction/BlockBreaking（挖方块用的，竞
   技场大概不需要挖方块所以可以不切）。

下一步：我准备基于这些发现给你一个具体方案（独
立
ChunkManager+专属渲染驱动+高度场同步+M键接线的
完整设计），要不要我现在整理成计划给你确认？
- 看了 TerrainMeshGenerator.generateSurfaceMesh()——确认只要 surfaceMaterials[x][z] 设成 DIRT（非 NONE），且 HeightMap
  每个顶点都设成同一个值，生成的网格就是一整块严格水平的平面，没有拼接缝隙问题
- 看了 CameraBlockCulling——它读的是 chunk.getBlockId()（体素层），跟地形网格是两套独立数据，互不冲突，竞技场切换时这个系统可以照常复用
- 看了 TerrainMaterial 枚举——DIRT/SAND/WATER/NONE，确认给竞技场用 DIRT 是合理默认

结论：方案A是可行的，没有发现新的阻塞问题。 ArenaWorld.generateArena() 需要补的逻辑就是：除了现在的 setBlock 填方块，还要对每个 chunk 调用
chunk.getSurfaceHeightMap().initializeFlat(固定高度) 和把 surfaceMaterials 全设成 DIRT（对应 y=TERRAIN_HEIGHT 那个平面），并且要在
chunkManager.getOrLoadChunk() 之后手动清掉 fillWithTestPattern() 留下的噪声污染。
。补充确认了两点:

- WorldModule.onInitialize() 里 chunkManager、textureManager、terrainMaterialFactory 都是绑在这一个模块实例上的字段,cameraBlockCulling 在
  onPostInitialize() 里也是拿这个 chunkManager 构造的——也就是说切世界时 cameraBlockCulling 得跟着重建/重新指向竞技场的
  chunkManager,否则摄像机穿墙透明化那套逻辑会继续按主世界的方块数据算。
- PlayerController 目前没有一个统一的"传送"方法,playerPosition/velocity/相机/puppet 位置是分散在几处手动同步的(比如 handlePlayerRevive()
  复活时的写法),现在这个坏掉的 enterArena() 也是照这个模式手动同步,漏了
  setWorldNode。这块需要做成一个统一的传送方法,一次性把这几处都同步到位,不然以后再加传送点又要重复漏东西。
- 总而言之：创造了按M可以进入的孤立空岛竞技场。
- 写了第一个怪：输入/mob1可以召唤，红色正方体，血量30点，以略慢于玩家的速度朝玩家走来，会躲避虚空，不会躲避彼此，会叠在一起
- 写了第一个怪物波：第一波3个，第二波4个，第三波出现一个体型略大速度略快血量略高的boss
- 第一个肉鸽奖励：在选择池里5选3：弹道+1，宽度+1，射击速度+50%，速度+5%，等
- 