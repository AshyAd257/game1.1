# 🎭 木偶编辑器使用教程

## 📋 概述

木偶编辑器是一个用于创建和编辑2D骨骼动画的工具。它支持关键帧动画、实时预览和直观的可视化编辑。

## 🚀 启动编辑器

### 方法1：直接运行编辑器
```bash
mvn exec:java -Dexec.mainClass="com.Hecate.puppet.editor.PuppetEditorApp"
```

### 方法2：运行Main程序后按I键
```bash
mvn exec:java -Dexec.mainClass="com.Hecate.Main"
```
然后在游戏中按 **I** 键打开编辑器。

## 🎮 编辑器界面

编辑器分为3个主要区域：

1. **Timeline面板**（顶部）- 显示时间轴和关键帧标记
2. **Inspector面板**（右侧）- 显示当前选中骨骼的属性
3. **视口**（中央）- 显示木偶的3D预览

## ⌨️ 快捷键

### 视图控制
- **左键拖动** - 旋转相机视角
- **滚轮** - 缩放视图
- **1-9, 0** - 选择对应的骨骼（1=Body, 2=Head, 等）

### 动画控制
- **K** - 在当前时间录制关键帧（记录所有骨骼的当前状态）
- **Space** - 播放/暂停动画
- **ESC** - 关闭编辑器
- **F11** - 全屏切换

### 部件调整
- **拖动滑条** - 调整选中部件的宽度/高度
- 滑条位于Inspector面板中

## 📝 创建动画的步骤

### 第1步：调整初始姿势
1. 按 **1** 选择Body（躯干）
2. 使用Inspector面板的滑条调整尺寸（可选）
3. 按 **2-0** 选择其他骨骼并调整

### 第2步：录制第一个关键帧（0秒）
1. 确认时间为 0.00s（查看Timeline面板）
2. 调整所有骨骼到起始姿势
3. 按 **K** 录制关键帧

控制台会显示：
```
========================================
[录制] ✓ 已录制关键帧 @ 0.00秒
[录制] 当前动画片段共有 10 个关键帧
[录制] 动画时长: 0.00秒
========================================
```

### 第3步：调整骨骼姿势
- 使用鼠标拖动旋转视角，观察木偶
- 选择不同骨骼并调整它们的尺寸
- 注意：当前版本需要通过代码调整旋转/位置，未来版本将支持直接拖动

### 第4步：录制更多关键帧
1. 等待时间推进（例如等到2.00s）
2. 调整骨骼到新的姿势
3. 再次按 **K** 录制关键帧

### 第5步：播放动画
1. 按 **Space** 播放动画
2. Timeline上会出现黄色标记，表示关键帧位置
3. 红色播放头会移动，显示当前播放时间

控制台会显示：
```
========================================
[播放] ▶ 开始播放动画
[播放] 动画名称: TestClip
[播放] 关键帧数: 20
[播放] 动画时长: 3.50秒
[播放] 循环模式: 开启
========================================
```

### 第6步：调整和完善
1. 按 **Space** 暂停
2. 调整姿势
3. 按 **K** 在当前时间添加新关键帧
4. 再次按 **Space** 播放查看效果

## 🎯 骨骼列表

木偶由10个骨骼组成：

| 数字键 | 骨骼名称 | 颜色 | 说明 |
|--------|----------|------|------|
| 1 | Body | 蓝色 | 躯干（根骨骼） |
| 2 | Head | 黄色 | 头部 |
| 3 | LeftUpperArm | 浅红 | 左大臂 |
| 4 | LeftLowerArm | 深红 | 左小臂 |
| 5 | RightUpperArm | 浅绿 | 右大臂 |
| 6 | RightLowerArm | 深绿 | 右小臂 |
| 7 | LeftUpperLeg | 粉红 | 左大腿 |
| 8 | LeftLowerLeg | 中红 | 左小腿 |
| 9 | RightUpperLeg | 淡绿 | 右大腿 |
| 0 | RightLowerLeg | 中绿 | 右小腿 |

## 💡 技巧和最佳实践

### 1. 关键帧间隔
- 建议每隔 0.5-2.0 秒录制一个关键帧
- 太密集：文件变大，调整困难
- 太稀疏：动画不够流畅

### 2. 层级动画
- 父骨骼的运动会影响所有子骨骼
- 例如：旋转Body会带动所有肢体一起旋转
- 利用这个特性可以创建复杂的联动效果

### 3. 插值特性
- 系统会自动在关键帧之间进行插值
- 位置/缩放：线性插值
- 旋转：球面线性插值（Slerp）
- 尺寸：线性插值

### 4. 查看控制台
编辑器会在控制台输出详细信息：
- 录制关键帧的时间和数量
- 播放/暂停状态
- 错误提示

## 🐛 常见问题

### Q: 按K没有反应？
A: 检查控制台是否显示错误信息。确保：
- 编辑器已正确初始化
- 动画系统已启动

### Q: 按Space提示"无法播放"？
A: 需要先录制至少1个关键帧。按K录制当前姿势。

### Q: Timeline上看不到关键帧标记？
A:
- 检查关键帧时间是否在0-10秒范围内
- 尝试重新打开编辑器（按I两次）

### Q: 如何删除关键帧？
A: 当前版本暂不支持删除。建议：
- 重启编辑器清空所有关键帧
- 或在不需要的时间点不录制关键帧

### Q: 动画循环播放吗？
A: 是的，默认开启循环模式。动画会从头到尾不断重复播放。

## 🔧 高级功能（未来版本）

计划中的功能：
- [ ] 直接拖动骨骼调整位置/旋转
- [ ] 关键帧删除和编辑
- [ ] 动画导出为JSON
- [ ] 多个动画片段管理
- [ ] 洋葱皮模式（显示前后帧）
- [ ] 反向动力学(IK)
- [ ] 纹理贴图支持

## 📚 示例工作流

### 创建一个挥手动画

```
时间 0.00s:
  - 录制初始姿势（手臂自然下垂）
  - 按K录制

时间 0.50s:
  - 旋转RightUpperArm（右大臂）向上
  - 按K录制

时间 1.00s:
  - 继续旋转RightUpperArm到最高点
  - 轻微旋转RightLowerArm（右小臂）
  - 按K录制

时间 1.50s:
  - 旋转RightUpperArm回到中间位置
  - 按K录制

时间 2.00s:
  - 恢复到初始姿势
  - 按K录制

按Space播放 - 查看挥手效果！
```

## 🎨 创意建议

尝试制作这些动画：
- 走路循环
- 跳跃
- 攻击动作
- 受伤后退
- 待机呼吸动画

祝你创作愉快！🎉
