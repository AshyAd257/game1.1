# 中文字体安装步骤

## 方法1：使用BMFont工具生成（推荐）

### 1. 下载BMFont
- 下载地址：http://www.angelcode.com/products/bmfont/
- 解压并运行 `bmfont.exe`

### 2. 配置字体
1. 点击 `Options > Font Settings`
   - 选择字体：**Microsoft YaHei** (微软雅黑) 或 **SimHei** (黑体)
   - Size: **16-24** (根据需要调整)
   - 勾选 **Bold** (可选)

2. 点击 `Options > Export Options`
   - Bit depth: **32**
   - Width: **512** (或 1024)
   - Height: **512** (或 1024)
   - Font descriptor: **Text**
   - Textures: **png**

### 3. 选择字符
1. 点击 `Edit > Select chars from file`
2. 创建一个文本文件 `chars.txt`，内容包含所有你需要的中文字符：
```
快捷键显示隐藏编辑器退出全屏切换选择骨骼调整宽度高度录制关键帧空格播放暂停鼠标左键拖拽旋转视角滚轮缩放滑条尺寸
位置缩放部件名称属性值面板时间帧数
ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789
+-*/=()[]{},.;:!?<>|&@#$%^~_\'"
```

3. 选择这个文件导入字符

### 4. 生成字体文件
1. 点击 `Options > Save bitmap font as...`
2. 保存为 `ChineseFont.fnt`
3. 会生成两个文件：
   - `ChineseFont.fnt`
   - `ChineseFont_0.png`

### 5. 复制到项目
将这两个文件复制到：
```
src/main/resources/Interface/Fonts/
    ChineseFont.fnt
    ChineseFont_0.png
```

## 方法2：使用在线工具

1. 访问：https://snowb.org/
2. 上传中文字体（.ttf文件）
3. 配置参数并生成
4. 下载 .fnt 和 .png 文件
5. 复制到项目的 `src/main/resources/Interface/Fonts/` 目录

## 测试
运行游戏后，如果看到控制台输出：
- `>>> Loaded Chinese font` - 字体加载成功
- `>>> Using default font` - 未找到字体，使用默认字体

## 注意事项
- 字体文件大小取决于包含的字符数量
- 建议只包含需要显示的字符，减小文件大小
- 字体纹理尺寸（512/1024）根据字符数量选择
