修复了有时候按键会卡住的bug。这次给武器池打的地基如下：

新增枚举（com.Hecate.weapon）
- WeaponCategory（RANGED/MELEE）
- WeaponKind：步枪/狙击枪/加特林/散弹枪 + 迅捷剑/大剑/骑枪/镰刀/战锤，本地化key仿照BuffType的keyPrefix+".name"/".desc"
  模式（weapon.rifle.name等，具体文案还没填,后续要在localization/*.properties里加）

变体支持
- WeaponDefinition加了kind字段（默认null，不影响现有smg_01/flame_thrower/steampunk_gun三个已注册武器），同一个kind可以
  注册多个WeaponDefinition实例，对应你说的"每种武器不只一种,会有多种变体"

子弹体系扩展（为切到系统B铺路）
- ProjectileProfile.HitEffect加了pierceCount穿透次数（狙击枪用）
- ProjectileProfile加了paintAlongPath/pathPaintInterval沿途涂墨字段（狙击枪弹道留痕、镰刀甩墨用）
- ProjectileProfile.VisualConfig加了texturePath贴图路径字段，新增texturedBox()工厂方法——对应你说的"抽象成一个大方块,贴
  图糊上去,以后需要多贴图再拆"
- ProjectileProfile.ArcType加了MELEE_SWING占位值（剑类挥砍用，当前物理更新还是落回直线，具体弧形轨迹待填）
- Projectile.hit()按pierceCount决定命中后是否继续存活；update()里加了沿途涂墨的计时器逻辑

远近战分支
- Weapon加了kind字段和isMelee()，PlayerController未来据此字段分支（远程/近战都在"持有武器"分支内部区分,不新增输入层第
  三态）
- 新增MeleeWeapon抽象类（继承Weapon），提供isBlockingActive()/getBlockDamageReduction()查询钩子——现在默认返回false/1.0
  ，不改变任何现有行为
- MonsterManager.checkContactDamage()接入了这个查询钩子（通过getCurrentWeapon()拿到武器判断instanceof），伤害结算逻辑
  不变，只是多了一层可以后续填数值的减免计算

骑枪/战锤/镰刀单独设计
- 三个独立骨架类：LanceChargeAbility（冲锋接管移动）、WarhammerSlamAbility（环形冲击波）、ScytheSpinAbility（三段状态
  机：蓄力→旋转→虚弱），都只搭了生命周期方法框架，没有接入PlayerController

全部改动跑过javac编译验证，包括依赖这些类的4个测试文件，没有破坏现有代码。这次没有触碰PlayerController的输入分发逻辑、
没有注册任何具体武器数值、没有加任何贴图资源文件——都是"预留空间"，具体数值和贴图留给下一步。
bug修复如下：
onAction() 里有 if (isDead)
return;，这会把死亡期间的所有按键事件都吞掉，包括松开键的事件。如果玩家死的瞬间正按着 WASD
或鼠标左键，那个按键状态就会永久卡在 true，因为松开事件也被挡住了。

handlePlayerRevive() 原来只重置了位置、速度、跳跃/着地状态，没清空这些按键标志位。复活后角色带着"卡住"的 moveDirection
继续走，isLeftButtonPressed 卡住的话还会触发连发开火逻辑——正是你说的"自己动和射击"。

这个 bug 其实一直都在（死亡机制里没做输入清理），只是加了 buff
选择流程后死亡→复活的循环变得更频繁，才明显暴露出来。有意思的是 buff 选择那条路径本身是对的：showBuffSelection()
解锁前调用了 resetInputStates()，复活路径却漏了这一步。

修复很直接：在 handlePlayerRevive()（PlayerController.java:742）开头加了一行
resetInputStates()，复用已有的重置逻辑（清空
moveDirection、isShiftPressed、isLeftButtonPressed、isRightButtonPressed、连发计时器）。已经单独编译验证
PlayerController.java 通过，无编译错误。