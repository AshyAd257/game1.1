涂墨系统完整实现报告

一、系统架构概览

涂墨系统采用分层架构 + 阵营抽象设计，核心组件包括：

核心组件关系图

[武器系统 Gun1/Gun2]
↓
[涂墨触发层 FlameParticle/Projectile]
↓
[网格管理层 SparseGridManager] ← [阵营系统 FactionRegistry]
↓
[数据存储层 GridRegion → GridCell]
↓
[渲染层 RegionMeshRenderer + InkGlowRenderer]
↓
[颜色解析器 ColorResolver] ← [观察者视角]

  ---
二、核心实现细节

1. 数据存储层

GridCell (网格单元)

- 位置: com.Hecate.ink.GridCell
- 职责: 表示 0.2m × 0.2m 的地面格子
- 核心字段:
  int factionId;       // 阵营ID（客观归属）
  float timestamp;     // 涂墨时间戳
  float intensity;     // 墨水强度 (0.0-1.0)
  boolean ignited;     // 是否点燃状态
- 关键方法:
    - ink(factionId, time): 涂墨
    - ignite(time): 点燃（10秒后降级为普通涂墨）
    - update(time, decayTime, igniteTime): 处理墨水衰减（60秒消退）
    - getSpeedMultiplier(playerFactionId): 计算移动速度倍率
        - 己方涂墨: 1.6x 速度（点燃 2.0x）
        - 敌方涂墨: 0.3x 速度（大幅减速）

GridRegion (区域管理)

- 位置: com.Hecate.ink.GridRegion
- 职责: 管理 16×16 格子块（3.2m × 3.2m）
- 优化机制:
    - 脏标记 (dirty): 仅在变化时重新渲染
    - 快速跳过 (hasAnyInk): 跳过空区域
    - 边界缓存 (cachedBoundaries): 用于光晕渲染

SparseGridManager (稀疏网格管理器)

- 位置: com.Hecate.ink.SparseGridManager
- 核心设计:
    - 稀疏存储: 仅存储有墨水的区域 (HashMap)
    - 分块管理: 16×16 格子为一个 Region
    - 时间切片: 每帧只更新有墨水的区域
- 关键常量:
  GRID_SIZE = 0.2f;              // 格子大小
  inkDecayTime = 60.0f;          // 墨水60秒消退
  igniteDecayTime = 10.0f;       // 点燃10秒后降级
- 核心接口:
  void inkCircle(Vector3f pos, float radius, int factionId);
  int igniteCircle(Vector3f pos, float radius);
  float getSpeedMultiplier(Vector3f playerPos, int playerFactionId);

  ---
2. 阵营系统

FactionRegistry (阵营注册表)

- 位置: com.Hecate.ink.FactionRegistry
- 职责: 管理阵营定义和关系
- 内置阵营:
  NONE = 0;                      // 空地
  LIGHT_DEFAULT = 1;             // 光属性默认阵营
  DARK_DEFAULT = 2;              // 暗属性默认阵营
- 关系枚举 (Relation):
    - SELF: 己方
    - ALLY: 盟友
    - ENEMY: 敌方
    - NEUTRAL: 中立

FactionDef (阵营定义)

- 位置: com.Hecate.ink.FactionDef
- 核心属性:
  int factionId;                 // 阵营ID
  VisualLineage visualLineage;   // 视觉血统 (LIGHT/DARK)
  ColorRGBA baseHue;             // 基础色相
  String name;                   // 阵营名称
- VisualLineage (视觉血统):
    - LIGHT (0): 光属性
    - DARK (1): 暗属性

  ---
3. 颜色解析系统

ColorResolver (颜色解析器)

- 位置: com.Hecate.ink.ColorResolver
- 设计思路: 把客观归属数据翻译成主观视觉体验

核心方法:
ColorRGBA resolve(int factionId, float intensity,
int observerFactionId, boolean inCombat)

颜色计算流程:
1. 中性态 (非战斗):
   - 光领地: 基础色 × NEUTRAL_LIGHT_BRIGHTNESS (1.0)
   - 暗领地: 基础色 × NEUTRAL_DARK_BRIGHTNESS (0.8)
   - 所有玩家看到的颜色一致
2. 战斗态:
   - 看己方领地: 保持中性色
   - 暗看光: 加 Bloom（刺眼）
    - 增亮幅度: BLOOM_INTENSITY_MAX = 1.2
    - 实现: color + bloomStrength
      - 光看暗: Darken（昏暗）
        - 降暗幅度: DARKEN_INTENSITY_MAX = 0.7
    - 实现: color × (1 - darkenStrength)

预留钩子:
- 两个 TODO 标记在 applyBloom() 和 applyDarken() 中
- 注释: "这些数值需要实机调整"

  ---
4. 渲染系统

RegionMeshRenderer (区域网格渲染器)

- 位置: com.Hecate.ink.RegionMeshRenderer
- 性能优化方案:
    - 旧方案: 256个格子 = 256个 Geometry = 256次 Draw Call
    - 新方案: 1个 Region = 1个 Geometry = 1次 Draw Call（256倍提升）

技术实现:
1. 动态纹理烘焙:
   - 16×16 像素纹理 (可配置 TEXTURE_SCALE)
   - 每个格子对应 1 个像素
   - 更新时只修改纹理像素，无需重建 Geometry
2. 地形拟合:
   - 8×8 网格细分 (MESH_SUBDIVISIONS)
   - 顶点采样地形高度 (CollisionManager.getTerrainHeightAt)
   - 渲染高度偏移: RENDER_HEIGHT_OFFSET = 0.22f
3. 光属性领地渐变效果:
   - 中心: 纯白色 (#FFFFFF)
   - 中间: 金黄色 (#FFC700，基础色)
   - 边缘: 橘红色 (#FF5C00，半透明)
   - 实现: applyLightFactionGradient() 计算到边缘距离
4. 观察者视角切换:
   setObserverFactionId(int factionId);
   setObserverInCombat(boolean inCombat);
   - 切换时标记所有 Region 为脏，触发重新渲染

InkGlowRenderer (光晕渲染器)

- 位置: com.Hecate.ink.InkGlowRenderer
- 设计原则:
    - 每个边界格子独立生成一小片光晕 quad
    - 朝外方向由 8 邻域非己方格子平均向量决定
    - 纵向渐变（靠近格子实色，远离格子透明）

光晕参数:
GLOW_WIDTH = GRID_SIZE;           // 光晕宽度 = 格子大小
GLOW_HEIGHT = 0.15f;              // 向外延伸高度（米）
RENDER_HEIGHT_OFFSET = 0.24f;    // 比涂墨略高，避免 z-fighting

渲染逻辑:
1. 检测边界格子 (GridRegion.getBoundaries())
2. 为每个边界格子生成 quad (4顶点 + 2三角形)
3. 合并为单个 Mesh，减少 Draw Call
4. 复用 ColorResolver 计算光晕颜色

  ---
三、涂墨触发机制对比

Gun1 (蒸汽朋克枪) - FlameParticle 系统

发射方式:
- FlameRenderer.emitFlame(): 发射 5 个粒子/发
- 物理模拟: 重力 (-15)、空气阻力、随机扰动
- 弹道: 抛物线飞行

涂墨时机:
- 仅在粒子落地时涂墨 (FlameParticle.java:145-147)
  if (onGround && !hasInked) {
  gridManager.inkCircle(surfacePos, 0.5f, this.factionId);
  hasInked = true;
  }
- 命中怪物时不涂墨，粒子直接消失

涂墨参数:
- 半径: 固定 0.5 米
- 偏移: 地面表面 +0.22 格

  ---
Gun2 (狙击枪) - Projectile 系统

发射方式:
- Projectile 对象 + ProjectileManager 管理
- 弹道: 近似平直（重力 -2.0f）
- 物理: 简化弹道（仅重力 + 衰减）

涂墨时机:
- 飞行过程中持续涂墨 (ProjectileManager.java:85-93)
  projectile.pathPaintTimer += tpf;
  if (projectile.pathPaintTimer >= pathPaintInterval) {
  gridManager.inkCircle(newPos, radius, projectile.getTeamId());
  projectile.pathPaintTimer = 0;
  }
- 涂墨间隔: 0.15 秒
- 命中怪物后继续飞行并涂墨（穿透机制）
- 最大飞行距离: 6 格

涂墨参数:
- 半径: 可配置 0.4 米 (stats.getInkRadius())
- 直接在子弹当前位置涂墨

  ---
对比总结

┌──────────┬────────────────────────┬────────────────────────────────┐
│   特性   │   Gun1 (蒸汽朋克枪)    │         Gun2 (狙击枪)          │
├──────────┼────────────────────────┼────────────────────────────────┤
│ 涂墨系统 │ FlameParticle          │ Projectile + ProjectileManager │
├──────────┼────────────────────────┼────────────────────────────────┤
│ 涂墨时机 │ 仅落地时               │ 飞行全程（每0.15秒）           │
├──────────┼────────────────────────┼────────────────────────────────┤
│ 命中怪物 │ 不涂墨，粒子消失       │ 继续飞行并涂墨（穿透）         │
├──────────┼────────────────────────┼────────────────────────────────┤
│ 涂墨半径 │ 固定 0.5m              │ 可配置 0.4m                    │
├──────────┼────────────────────────┼────────────────────────────────┤
│ 墨迹形态 │ 散点状（多个落点）     │ 连续线状（弹道痕迹）           │
├──────────┼────────────────────────┼────────────────────────────────┤
│ 弹道类型 │ 抛物线（重力-15）      │ 近似平直（重力-2）             │
├──────────┼────────────────────────┼────────────────────────────────┤
│ 物理模拟 │ 粒子物理（扰动、阻力） │ 简化弹道（仅重力+衰减）        │
└──────────┴────────────────────────┴────────────────────────────────┘

底层共同点:
两者最终都调用同一个接口：
SparseGridManager.inkCircle(position, radius, factionId)

  ---
四、预留钩子

1. 事件系统钩子 (EventBus)

- 位置: com.Hecate.event.EventBus
- 已有事件:
    - PaintEvent: 涂墨事件（未在当前代码中使用）
- 预留接口:
  eventBus.subscribe(PaintEvent.class, listener);
  eventBus.publish(new PaintEvent(...));

2. 颜色解析钩子 (ColorResolver)

- TODO 标记:
    - applyBloom(): Bloom 强度调整
    - applyDarken(): Darken 强度调整
- 可调参数:
  NEUTRAL_LIGHT_BRIGHTNESS = 1.0f;    // 光领地中性态亮度
  NEUTRAL_DARK_BRIGHTNESS = 0.8f;     // 暗领地中性态亮度
  BLOOM_INTENSITY_MAX = 1.2f;         // Bloom 最大增亮幅度
  DARKEN_INTENSITY_MAX = 0.7f;        // Darken 最大降暗幅度

3. 观察者系统钩子

- PlayerController 集成点:
    - setObserverFactionId(): 切换玩家阵营
    - setObserverInCombat(): 切换战斗状态
- TODO 标记 (GridDebugRenderer):
    - 观察者信息应从 PlayerController 自动获取，而非硬编码

4. 网格管理钩子

- 世界切换接口:
  RegionMeshRenderer.setGridManager(SparseGridManager newGridManager);
    - 用于竞技场切换、副本切换等场景
    - 自动清除旧世界墨水数据

5. 性能调优钩子

- 可配置参数 (RegionMeshRenderer):
  TEXTURE_SCALE = 1;              // 纹理分辨率倍率
  MESH_SUBDIVISIONS = 8;          // 网格细分精度
  RENDER_HEIGHT_OFFSET = 0.22f;   // 渲染高度偏移
- 可配置参数 (SparseGridManager):
  MAX_UPDATES_PER_FRAME = 10;     // 每帧最多更新区域数

  ---
五、待改进之处

1. 性能优化

问题 1: 墨水衰减计算开销

- 现状: 每帧遍历所有有墨水的 Region 和 Cell 计算衰减
- 影响: 墨水区域多时 CPU 占用高
- 建议优化:
    - 实现时间轮算法，按墨水消退时间分桶
    - 仅在时间到达时批量处理过期墨水
    - 预期性能提升: 50-70%

问题 2: 边界检测算法

- 现状: calculateDistanceToEdge() 每次都遍历 8 邻域
- 影响: 光属性领地渲染时 CPU 占用较高
- 建议优化:
    - 缓存边界信息，仅在 Region dirty 时重新计算
    - 使用距离场 (Distance Field) 预计算
    - 预期性能提升: 30-40%

问题 3: 跨 Region 访问优化不足

- 现状: getCellFast() 支持跨 Region 访问，但对角线需要 3 次查询
- 影响: 光晕渲染、渐变效果计算时性能损耗
- 建议优化:
    - 实现 8 邻居 Region 缓存 (3x3 网格)
    - 预期性能提升: 20-30%

  ---
2. 功能完整性

问题 1: 事件系统未启用

- 现状: EventBus 和 PaintEvent 已实现但未使用
- 影响: 无法通过事件监听涂墨行为
- 建议:
    - 在 SparseGridManager.inkCircle() 中发布 PaintEvent
    - 支持模块化扩展（如音效、粒子特效、统计面板）

问题 2: 观察者系统耦合度高

- 现状: observerFactionId 和 observerInCombat 硬编码在渲染器中
- 影响: 多玩家模式难以扩展
- 建议:
    - 抽象 Observer 接口
    - 支持多个观察者独立渲染（分屏模式）

问题 3: 墨水覆盖策略单一

- 现状: 后涂的墨水直接覆盖先涂的墨水
- 影响: 无法实现混合色、中和、侵蚀等高级效果
- 建议:
    - 支持多层墨水叠加
    - 实现墨水混合规则（如光暗墨水中和为中性灰）

  ---
3. 代码质量

问题 1: 魔法数字过多

- 现状: 0.22f、0.5f、1.6f 等数值散布在代码中
- 影响: 难以调优，参数含义不明确
- 建议:
    - 集中定义为 InkConfig 类的常量
    - 支持运行时热重载配置文件

问题 2: 缺少单元测试

- 现状: 无测试覆盖
- 影响: 重构风险高，边界情况未验证
- 建议:
    - 为 SparseGridManager 添加核心逻辑测试
    - 为 ColorResolver 添加颜色计算测试
    - 为坐标转换函数添加边界测试（负坐标、跨 Region）

问题 3: 日志系统不完善

- 现状: 关键操作无日志输出
- 影响: 难以追踪墨水异常、性能瓶颈
- 建议:
    - 添加 DEBUG 级别日志（墨水创建、衰减、清除）
    - 添加性能统计日志（每帧更新区域数、总墨水格子数）

  ---
4. 扩展性

问题 1: 阵营系统扩展受限

- 现状: VisualLineage 仅支持 LIGHT/DARK 两种
- 影响: 无法添加第三种视觉血统（如"中性"、"混沌"）
- 建议:
    - 将 VisualLineage 改为可扩展的枚举或接口
    - 支持自定义视觉效果规则

问题 2: 地形拟合依赖硬编码

- 现状: RENDER_HEIGHT_OFFSET = 0.22f 在多处硬编码
- 影响: 不同地形高度不一致时出现 z-fighting
- 建议:
    - 动态计算偏移量（基于地形法线和斜率）
    - 支持自适应高度调整

问题 3: 渲染效果缺少配置接口

- 现状: 光晕宽度、渐变效果等无法动态调整
- 影响: 美术调优需要修改代码并重新编译
- 建议:
    - 提供 InkRenderConfig 配置文件
    - 支持实时预览调整效果

  ---
5. 兼容性

问题 1: 大世界坐标精度问题

- 现状: 使用 float 存储世界坐标
- 影响: 世界尺寸 > 10km 时精度损失严重
- 建议:
    - 对超大世界使用分区坐标系统
    - 区域坐标使用 int，仅局部计算使用 float

问题 2: 跨平台渲染兼容性

- 现状: 纹理格式使用 Image.Format.RGBA8
- 影响: 某些移动端 GPU 不支持
- 建议:
    - 检测平台支持的纹理格式
    - 提供 Fallback 方案（如 RGB565）
- 涂墨触发入口

    - Gun1: FlameParticle.java:145-147
    - Gun2: ProjectileManager.java:85-93
    - 底层接口: SparseGridManager.inkCircle()

  墨水衰减逻辑

    - 时间戳更新: GridCell.update() (GridCell.java:89-103)
    - 区域批量更新: SparseGridManager.update() (SparseGridManager.java:69-91)

  颜色计算核心

    - 主入口: ColorResolver.resolve() (ColorResolver.java:44-74)
    - Bloom 效果: ColorResolver.applyBloom() (ColorResolver.java:156-168)
    - Darken 效果: ColorResolver.applyDarken() (ColorResolver.java:182-194)

  渲染优化核心

    - 纹理烘焙: RegionRenderer.updateTexture() (RegionMeshRenderer.java:406-449)
    - 光晕生成: RegionGlow.createGlowMesh() (InkGlowRenderer.java:245-354)

  移动速度影响

    - 速度倍率计算: GridCell.getSpeedMultiplier() (GridCell.java:126-152)
    - 查询接口: SparseGridManager.getSpeedMultiplier() (SparseGridManager.java:297-303)