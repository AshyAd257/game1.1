已经有一个单朝向半砖方块(xx，模型halfbrick.glb，Y∈[-0.5,0]贴地放置)。用户想要类MC的台阶体验：

1. 横放半砖(下半/上半)可以在同一格叠放凑成完整方块。
2. 新增竖放半砖，支持左/右/前/后四个朝向，同样可以叠放凑成完整方块。
3. 美术画贴图的约定：始终画一张完整方块贴图(和cube_uv_template.png一样)，半砖只是采样这张贴图的一半区域；叠满整格时两
   个半砖分别显示贴图的两半，拼起来等价于完整方块。这个规则要通过通用注册方法对未来任何新半砖材质自动成立，不依赖人工每次
   小心复制。
4. 打掉叠满的双层半砖：一次打完直接清空(不做"先退化成单层再打一次"的分级破坏)。
5. 朝向选择：修饰键(Alt)切换"横放模式/竖放模式"；同一模式内的具体朝向(上下 或
   左右前后)由点击命中点在目标格内的局部位置自动判断。

现有架构调研确认：整个方块系统(存储/渲染/碰撞)都基于"一个blockId=一个模型+一个碰撞盒"的假设，靠不同blockId编码不同状态
(wood1的Axis朝向是这么做的)。延续这个模式是改动最小、风险最低的路径——不需要动Chunk存储结构、不需要动WorldModule渲染管
线、不需要让CollisionManager支持"一格多AABB"。

涉及文件与改动

1. generate_block_shapes.py — 生成6个朝向的几何体+UV

新增一个参数化的build_slab(axis,
sign)函数(axis∈{'x','y','z'}，sign∈{-1,+1}表示沿该轴的哪一半)，替代手写6份几何体代码。规则：

- 几何范围：沿axis方向的坐标范围被砍半(sign=-1→[-0.5,0]，sign=+1→[0,0.5])，另外两轴维持满格[-0.5,0.5]。
- UV裁剪规则(对应用户"横放裁上下，竖放裁左右"的要求，本质是同一个机制在不同轴上的应用)：
    - 法线平行于axis的两个面(比如Y轴分割时的TOP/BOTTOM)是"端盖"面，不裁剪，直接用cube上对应面的完整UV格子(和现有build_ha
      lfbrick对TOP/BOTTOM的处理一致)。
    - 法线垂直于axis的四个面是"侧面"，需要裁剪——沿axis方向裁一半，裁剪落在贴图的U还是V轴、方向是否需要取反，取决于该面自
      身的UV参数化方式(需要对着build_cube()里FRONT/BACK/LEFT/RIGHT/TOP/BOTTOM各面的顶点-UV对应关系逐个核对，用之前验证cube/w
      edge/halfbrick时同款的"解析glb校验UV bbox"脚本做数值验证，确保：①同一材质的bottom+top(或left+right、front+back)两个变
      体的裁剪区间正好互补拼满[0,1]，不重叠不留缝；②裁剪结果里没有意外的镜像——BACK/RIGHT这类"看向轴反向"的面即使裁的是同一半
      区间，取的U/V子区间在数值上会自然落在"另一侧"，这是该面自身UV参数化decided的自然结果，不是bug，也不需要特殊处理消除)。
    - 扩展现有CrossLayout.cell_uv_crop，让它同时支持u_frac和v_frac两个裁剪参数(目前只有v_frac)。
- 画布复用：build_slab内部复用cube_layout()(即完全等同于cube.glb的画布尺寸和格子划分)——这样保证美术画的"一张完整方块贴
  图"可以不做任何修改直接套用在所有横放/竖放半砖上，天然满足"叠满=完整方块"的规则，不需要为每种半砖单独裁一张缩小贴图。
- 输出6个几何体文件(形状原型命名，不带材质名，延续cube/wedge/halfbrick的命名惯例)：slab_bottom.glb slab_top.glb
  slab_left.glb slab_right.glb slab_front.glb
  slab_back.glb，放进src/main/resources/blocks/。"叠满整格"这个变体不需要新几何体，直接复用现成的cube.glb。
- 校验脚本沿用之前验证cube/wedge/halfbrick时的手法(手工解析glb的JSON+BIN chunk，读UV顶点坐标算bbox)，新增校验点：botto
  m/top的v范围互补、left/right的u范围互补、front/back的u范围互补(具体是U还是V由该方向对应的裁剪面决定)。

2. com.Hecate.block.SlabOrientation(新枚举)

public enum SlabOrientation { BOTTOM, TOP, LEFT, RIGHT, FRONT, BACK, DOUBLE }
定义互补配对关系(用于合并判断)：BOTTOM↔TOP，LEFT↔RIGHT，FRONT↔BACK。

3. Block.java — 新增半砖族相关字段

新增两个字段(仅半砖族方块非null)：slabFamily(String，如"xx"，同一材质的7个变体共享)、slabOrientation(SlabOrientation)
。新增对应getter和isSlabPart()判断方法。不改动现有构造器行为(新字段通过专门给半砖族用的构造器/setter注入，其余方块保持
null，向后兼容)。

4. BlockShapeRegistry.java — 无需新增机制，复用现有registerShapeWithOffset

6个方向变体+1个双层变体的碰撞盒(单位=格子)：
- BOTTOM: size(1,0.5,1) offset(0,-0.25,0) — 复用现有"xx"的既有配置
- TOP: size(1,0.5,1) offset(0,+0.25,0)
- LEFT: size(0.5,1,1) offset(-0.25,0,0)
- RIGHT: size(0.5,1,1) offset(+0.25,0,0)
- BACK: size(1,1,0.5) offset(0,0,-0.25)
- FRONT: size(1,1,0.5) offset(0,0,+0.25)
- DOUBLE: size(1,1,1) offset(0,0,0)(即registerShape默认满格)

5. BlockRegistry.java — 新增registerSlabFamily()通用注册方法+合并解析方法

public void registerSlabFamily(String baseId, String name, boolean solid, float hardness, String texturePath) {
// 依次注册 baseId_bottom/_top/_left/_right/_front/_back（模型分别指向slab_xxx.glb，
// 贴图统一是texturePath，skipAutoScale=true）+ baseId_double（模型cube.glb，同一张texturePath）
// 同时调用shapeRegistry注册上面7套碰撞盒
}
以后新增半砖材质只需一行：registerSlabFamily("xx", "半砖xx", true, 0.8f,
"blocks/bluetest.png")，替换掉现在手写的单一"xx"注册代码。

新增合并/放置解析方法(供BlockInteraction调用，与现有resolvePlacementVariant并列，互不影响，wood1路径不受影响)：
public String resolveSlabPlacement(String slabFamily, SlabOrientation requested, String currentBlockId) {
// currentBlockId为air → 直接返回 slabFamily+"_"+requested对应的单变体id
// currentBlockId是同族的互补朝向 → 返回 slabFamily+"_double"
// currentBlockId是同族的相同朝向（重复放置）或不同族/非半砖内容 → 返回null（拒绝放置）
}

6. BlockInteraction.java — 放置逻辑分支+朝向推断

placeBlock签名改为接收一个"竖放模式"布尔值(向后兼容：保留旧签名作为默认false的重载)。核心改动：
- 若手持方块是半砖族(held.isSlabPart()或类似判断)，走新分支：根据命中点局部坐标(hitPoint -
  placePos)+竖放模式布尔值，推断SlabOrientation：
    - 非竖放模式：局部Y≥0→TOP，否则→BOTTOM。
    - 竖放模式：比较局部X、Z的绝对值谁更大决定分割轴，再由符号决定LEFT/RIGHT或FRONT/BACK。
    - 调用blockRegistry.resolveSlabPlacement(...)，返回null则放置失败，否则写入返回的id(单变体或双层变体)。
- 若手持方块不是半砖族，保留现有逻辑(必须air、走resolvePlacementVariant)完全不变。
-
breakBlock()/previewPlacementPosition()不需要改动(整格清空的语义天然满足"一次打完"的要求；预览框逻辑本来就只在目标格为
其它内容时的可放置性判断上工作，半砖的"能否合并"判断留给实际放置时处理，预览框沿用现有"是否air"的粗略判断即可，够用)。

7. PlayerControlModule.java — 新增Alt修饰键映射

新增按键映射(参照现有NormalMode/NormalModeAlt双键绑定Ctrl的写法)：
app.getInputManager().addMapping("SlabVerticalModeL", new KeyTrigger(KeyInput.KEY_LMENU));
app.getInputManager().addMapping("SlabVerticalModeR", new KeyTrigger(KeyInput.KEY_RMENU));
在onAction里维护一个isSlabVerticalMode布尔字段(按下置true/松开置false)，PlaceBlock分支调用blockInteraction.placeBlock(
blockId, isSlabVerticalMode)。Alt键目前完全未被占用，不会跟现有Shift(隐藏)/Ctrl(普通模式)冲突。

8. 示例注册替换

BlockRegistry.initializeDefaultBlocks()里把现在手写的单一"xx"注册(new Block(...) +
registerShapeWithOffset("xx",...))整体替换成一行：
registerSlabFamily("xx", "半砖xx", true, 0.8f, "blocks/bluetest.png");

不需要改动的部分(已确认)

- Chunk.java存储结构：新增的6个blockId字符串(xx_bottom等)不影响存档兼容性，旧存档字符串正常读取。
- WorldModule.java渲染管线：每个新变体依然是"一个blockId=一个模型+一个贴图"，完全复用现有createBlockGeometry/createCus
  tomModelGeometry路径(包括之前已经加好的skipAutoScale标记和外部贴图覆盖机制)，不需要改动。
- CollisionManager.java：碰撞盒查询接口getHalfExtents/getCenterOffset已经支持任意尺寸+偏移，7个变体各自注册好参数即可
  ，代码本身不用改。

验证方式

1. 运行python
   generate_block_shapes.py后，用之前验证cube/wedge/halfbrick时同款的手工glb解析脚本，检查6个slab_*.glb的顶点数/UV范围，
   重点验证bottom+top、left+right、front+back三对的UV裁剪区间互补(拼起来正好是[0,1]完整范围，不重叠、不留缝)。
2. 编译后进入游戏，/give xx拿到半砖，测试：
- 不按Alt点击方块顶面/侧面上下不同高度，确认能放出上/下半砖，贴图显示是完整贴图的对应一半。
- 按住Alt点击，确认能放出左/右/前/后半砖。
- 对着已有的单个半砖，用互补朝向叠放，确认变成完整方块外观(碰撞箱也变满格)，贴图整体等价于直接放一个用同一张贴图的cu
  be方块。
- 对着叠满的双层半砖挖一下，确认一次性变回空气。
- 碰撞箱：用F3打开碰撞箱显示，确认各朝向单层半砖的碰撞盒位置/尺寸和贴图外观吻合，没有悬浮/割裂。