# 网格渲染性能问题分析

## 问题根源

### 1. 架构问题：每个格子一个 Geometry
```
GridDebugRenderer 当前实现：
- 0.2m × 0.2m 的格子 = 1 个 Quad
- 1 个 Quad = 1 个 Geometry
- 1 个 Geometry = 1 个 Material
- 1 个 Material = 1 次 Draw Call
```

**示例计算**：
- 30米半径区域 ≈ 150m × 150m = 22500个格子
- = 22500 个 Geometry
- = 22500 次 Draw Call
- **这对任何渲染引擎都是灾难**

### 2. 性能瓶颈：强制刷新频率
```java
// 原代码：每30帧（0.5秒）刷新所有格子
private static final int FORCE_REFRESH_INTERVAL = 30;
```

每次刷新：
- 遍历所有 Region
- 遍历 Region 中所有格子 (32×32 = 1024)
- 调用 `colorResolver.resolve()` 计算颜色
- 更新 Material

**30米半径 × 每0.5秒刷新 = 卡死**

---

## 临时修复（已应用）

### 修改1：降低刷新频率
```java
private static final int FORCE_REFRESH_INTERVAL = 300; // 5秒刷新一次
```

**效果**：缓解卡顿，但治标不治本

### 修改2：禁用测试场景生成
大面积涂墨会创建大量 Geometry，暂时禁用自动生成

---

## 根本解决方案

### 方案A：合并网格（推荐）

#### 思路
一个 GridRegion (32×32格子) 用**一个 Mesh**，而不是 1024 个 Geometry

#### 实现方式

**选项1：Vertex Color**
```java
// 一个 Region = 一个 Mesh
Mesh regionMesh = new Mesh();
// 32×32 个格子 = 32×32 个 Quad = 4096 个顶点
float[] positions = new float[32 * 32 * 4 * 3]; // x,y,z
float[] colors = new float[32 * 32 * 4 * 4];    // r,g,b,a

// 为每个格子设置顶点颜色
for (int x = 0; x < 32; x++) {
    for (int z = 0; z < 32; z++) {
        GridCell cell = region.getCell(x, z);
        ColorRGBA color = colorResolver.resolve(...);

        // 设置这个格子的4个顶点颜色
        setQuadColor(colors, x, z, color);
    }
}

regionMesh.setBuffer(Type.Position, 3, positions);
regionMesh.setBuffer(Type.Color, 4, colors);
```

**优点**：
- 1个 Region = 1次 Draw Call（减少 1024倍）
- 颜色实时可变（修改 Color Buffer 即可）

**缺点**：
- 需要重写 GridDebugRenderer
- 需要自己管理 Mesh 的 Vertex Buffer

---

**选项2：Texture Atlas + 动态纹理**
```java
// 为每个 Region 生成一张 32×32 的纹理
BufferedImage regionTexture = new BufferedImage(32, 32, BufferedImage.TYPE_INT_ARGB);

// 更新纹理：遍历格子，写入像素
for (int x = 0; x < 32; x++) {
    for (int z = 0; z < 32; z++) {
        GridCell cell = region.getCell(x, z);
        ColorRGBA color = colorResolver.resolve(...);
        regionTexture.setRGB(x, z, colorToInt(color));
    }
}

// 上传到GPU
Texture2D tex = new Texture2D(new AWTLoader().load(regionTexture, false));
```

**优点**：
- 1个 Region = 1张纹理 = 1次 Draw Call
- 可以用 Shader 做特效

**缺点**：
- 纹理上传有性能开销
- 需要处理纹理分辨率和抗锯齿

---

### 方案B：视锥剔除 + LOD

#### 思路
只渲染玩家看得见的格子，远处格子降低更新频率

```java
// 获取玩家相机
Camera cam = app.getCamera();

// 只渲染视锥内的 Region
for (GridRegion region : allRegions) {
    if (isInFrustum(cam, region)) {
        // 计算距离
        float dist = region.getCenter().distance(cam.getLocation());

        if (dist < 10f) {
            // 近距离：每帧更新
            updateRegionMaterials(region);
        } else if (dist < 30f) {
            // 中距离：每5秒更新
            if (shouldUpdateThisFrame(region, 300)) {
                updateRegionMaterials(region);
            }
        } else {
            // 远距离：不更新或10秒更新一次
        }
    }
}
```

**优点**：
- 减少大部分不必要的更新
- 易于实现（在现有代码基础上改）

**缺点**：
- 仍然是每个格子一个 Geometry（Draw Call 多）

---

### 方案C：只在墨水变化时更新

#### 思路
不要定期强制刷新，改为"事件驱动"

```java
// 监听涂墨事件
eventBus.subscribe(PaintEvent.class, event -> {
    // 只更新被涂墨影响的格子
    Vector3f pos = event.getPosition();
    float radius = event.getRadius();

    // 标记受影响的 Region 为 dirty
    markRegionsAsDirty(pos, radius);
});

// 渲染时只更新 dirty 的 Region
public void update() {
    for (GridRegion region : dirtyRegions) {
        updateRegionMaterials(region);
        region.clearDirty();
    }
    dirtyRegions.clear();
}
```

**优点**：
- 不再定期全量刷新
- 只在实际变化时更新

**缺点**：
- 墨水衰减怎么办？（需要单独处理）

---

## 综合方案（推荐）

**阶段1：快速修复（已完成）**
- ✅ 降低刷新频率到 5秒
- ✅ 禁用大规模测试场景

**阶段2：架构重构（必须做）**
- 实现 **Vertex Color 方案**
- 一个 Region = 一个 Mesh
- 减少 1000倍 Draw Call

**阶段3：进一步优化（可选）**
- 视锥剔除
- 距离 LOD
- 事件驱动更新

---

## 性能目标

### 当前性能
- 30米半径 ≈ 22500个 Geometry
- 帧率：< 10 FPS（不可接受）

### 优化后目标
- 30米半径 ≈ 约20个 Region = 20个 Mesh
- 帧率：> 60 FPS（可接受）

---

## 下一步行动

1. **测试当前修复**
   - 用手动涂墨测试 ColorResolver
   - 观察小范围涂墨的性能

2. **决定是否重构**
   - 如果确实需要大地图战斗
   - 必须实现 Vertex Color 方案

3. **ColorResolver 调参**
   - 性能优化后再进行视觉调参
